---
import type { BookRecord } from "../types/books";

type Props = {
  book: BookRecord;
  number: number;
};

const { book, number } = Astro.props;
const entryUrl = `/books#${book.slug}`;
const hasLinks = Boolean(book.goodreadsUrl || book.amazonUrl);
const hasCover = Boolean(book.coverImage);
const numberLabel = String(number).padStart(2, "0");
---

<article class="book-entry" id={book.slug}>
  <div class:list={["entry-grid", !hasCover && "is-coverless"]}>
    {
      book.coverImage && (
        <div class="cover-shell">
          <img
            class="cover-image"
            src={book.coverImage}
            alt={`Cover of ${book.title}`}
            loading="lazy"
            decoding="async"
          />
        </div>
      )
    }

    <div class="entry-copy">
      <header class="entry-header">
        <div class="entry-heading-row">
          <div class="entry-number" aria-label={`Book ${number}`}>
            <span class="entry-number-label">Book</span>
            <span class="entry-number-value">{numberLabel}</span>
          </div>

          <div class="entry-heading-copy">
            <h2 class="entry-title">
              <a href={entryUrl}>{book.title}</a>
            </h2>
            {book.author && <p class="entry-author">{book.author}</p>}
          </div>
        </div>
      </header>

      {book.quote && <blockquote>{book.quote}</blockquote>}
      {book.myTake && <div class="entry-take prose"><p>{book.myTake}</p></div>}

      {
        hasLinks && (
          <div class="entry-links" aria-label={`External links for ${book.title}`}>
            {book.goodreadsUrl && (
              <a
                class="link-inline"
                href={book.goodreadsUrl}
                target="_blank"
                rel="noreferrer"
              >
                Goodreads
              </a>
            )}
            {book.amazonUrl && (
              <a
                class="link-inline"
                href={book.amazonUrl}
                target="_blank"
                rel="noreferrer"
              >
                Amazon
              </a>
            )}
          </div>
        )
      }
    </div>
  </div>
</article>

<style>
  .book-entry {
    position: relative;
    padding: 1.75rem 0;
    border-top: 1px solid var(--color-rule);
    scroll-margin-top: 5rem;
  }

  .entry-grid {
    display: grid;
    grid-template-columns: minmax(0, 7.5rem) minmax(0, 1fr);
    gap: 1.5rem 1.75rem;
    align-items: start;
  }

  .cover-shell {
    max-width: 7.5rem;
  }

  .entry-grid.is-coverless {
    grid-template-columns: 1fr;
  }

  .cover-image {
    width: 100%;
    aspect-ratio: 2 / 3;
    object-fit: cover;
    border-radius: 1rem;
    border: 1px solid rgba(32, 29, 26, 0.12);
    background: rgba(255, 255, 255, 0.72);
    box-shadow: var(--shadow-soft);
  }

  .entry-copy {
    display: grid;
    gap: 1rem;
    min-width: 0;
  }

  .entry-header {
    display: grid;
    gap: 0.35rem;
  }

  .entry-heading-row {
    display: grid;
    grid-template-columns: auto minmax(0, 1fr);
    gap: 1rem;
    align-items: start;
  }

  .entry-heading-copy {
    min-width: 0;
  }

  .entry-number {
    display: inline-grid;
    gap: 0.15rem;
    min-width: 4.2rem;
    padding: 0.55rem 0.65rem 0.5rem;
    border: 1px solid rgba(141, 91, 63, 0.18);
    border-radius: 999px;
    background:
      linear-gradient(180deg, rgba(255, 255, 255, 0.9), rgba(255, 255, 255, 0.68));
    box-shadow: var(--shadow-soft);
    text-align: center;
  }

  .entry-number-label {
    color: var(--color-subtle);
    font-size: 0.7rem;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .entry-number-value {
    font-family: var(--font-display);
    font-size: 1.35rem;
    line-height: 1;
    letter-spacing: -0.04em;
  }

  .entry-title {
    font-size: clamp(1.5rem, 2vw, 2rem);
  }

  .entry-title a {
    color: inherit;
    text-decoration: none;
    transition: color var(--transition-fast);
  }

  .entry-title a:hover,
  .entry-title a:focus-visible {
    color: var(--color-accent);
  }

  .entry-author {
    color: var(--color-muted);
    font-size: 1rem;
    font-weight: 600;
    letter-spacing: 0.01em;
  }

  .entry-take {
    max-width: 44rem;
  }

  .entry-links {
    display: flex;
    flex-wrap: wrap;
    gap: 0.85rem 1.1rem;
    font-size: 0.98rem;
  }

  @media (max-width: 760px) {
    .entry-grid {
      grid-template-columns: 1fr;
    }

    .entry-heading-row {
      grid-template-columns: 1fr;
      gap: 0.75rem;
    }

    .entry-number {
      justify-self: start;
    }

    .cover-shell {
      max-width: 6.25rem;
    }
  }
</style>
